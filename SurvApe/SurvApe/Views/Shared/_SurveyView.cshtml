









@model IEnumerable<SurvApe.Models.TrueOrFalseQuestion>

@*@{
    ViewBag.Title = "Index";
}


<fieldset>

<div class="editor-label">
    @Html.LabelFor(model => model.Text)
</div>
<div class="editor-field">
    @Html.EditorFor(model => model.Answer)
    @Html.ValidationMessageFor(model => model.Answer)
</div>

<p>
    <inputtype ="submit" value="Save" />
</p>
</fieldset>*@


@*<table>
    <thead>
        <tr>
            <th>Approved status</th>
        </tr>
    </thead>
    <tbody>
        @Html.EditorFor(x => x)
    </tbody>
</table>

<tr>
    <td>
        @Html.RadioButtonFor(x => x, "true") <span>Approved</span>
        @Html.RadioButtonFor(x => x, "false") <span>Not approved</span>
    </td>
</tr>*@




@*@using (Html.BeginForm())
{
    if (Model != null)

    {

        foreach (var item in Model)
        {
            {
                <asp:TrueFalseList ID="TrueFalse" DataTextField="AnswerGivenBool" DataValueField="AnswerGivenBool">
                    @Html.DisplayFor(modelItem => item.Text)
                    @Html.RadioButtonFor(modelItem => item.Answer, "false", new { id = "Answer" })
                    @Html.Label("false", "False")

                    @Html.RadioButtonFor(modelItem => item.Answer, "true", new { id = "Answer" })
                    @Html.Label("true", "True")
                    <button type="submit">OK</button>
                </asp:TrueFalseList>
            }

        }

    }
}*@












                    @*<table class="table">
                            <tr>
                                <th>

                                </th>
                                <th>
                                </th>
                            </tr>
                        @if (Model != null)

                        {

                            foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        <asp:TrueFalseList ID="TrueFalse" DataTextField="AnswerGivenBool" DataValueField="AnswerGivenBool">

                                            @*<div class="checkbox">
                                                <label><input type="checkbox" value="">true</label>
                                            </div>
                                            <div class="checkbox">
                                                <label><input type="checkbox" value="">false</label>
                                            </div>*@

                    @*@Html.DisplayFor(modelItem => item.Text)
                        @Html.CheckBox("Answer", new  { value = item.Answer, @checked = true })
                        @Html.LabelFor(model => model, "True")

                        @Html.CheckBox("Answer", new { value = item.Answer, @checked = false })
                        @Html.LabelFor(model => model, "False")*@

                    @*</asp:TrueFalseList>
                                    </td>
                                    <td>

                                    </td>
                                </tr>
                            }
                        }
                            </table>*@

                    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js"></script>
                    <script src="//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js"></script>

                    <script src="//ajax.aspnetcdn.com/ajax/jquery.validate/1.11.1/jquery.validate.min.js"></script>
                    <script src="//ajax.aspnetcdn.com/ajax/mvc/4.0/jquery.validate.unobtrusive.min.js"></script>

                    <script type="text/javascript">		</script>




